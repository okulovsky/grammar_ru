
from setuptools import setup, find_packages

setup(name='Mnist_container',
      version='1',
      packages=find_packages(),
      install_requires=[
      ],
      include_package_data = True,
      zip_safe=False)
